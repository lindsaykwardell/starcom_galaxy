<%= form_with(model: empire, url: url) do |form| %>
<% if empire.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(empire.errors.count, "error") %> prohibited this empire from being saved:</h2>

  <ul>
    <% empire.errors.each do |error| %>
    <li><%= error.full_message %></li>
    <% end %>
  </ul>
</div>
<% end %>

<div class="grid grid-cols-6 py-2">
  <%= form.label :empire_name, class: "col-span-2" %>
  <%= form.text_field :empire_name, class: "col-span-4 p-1 shadow" %>
</div>

<div class="grid grid-cols-6 py-2">
  <%= form.label :description, class: "col-span-2" %>
  <%= form.text_area :description, class: "col-span-4 p-1 shadow" %>
</div>

<div class="actions">
  <%= form.submit "Create Empire", class: "bg-yellow-700 hover:bg-yellow-800 px-6 py-2 mt-4 text-lg rounded shadow hover:shadow-lg transition duration-50 cursor-pointer" %>
</div>
<% end %>